interface:
  display_name: "Briefwright"
  short_description: "Build auditable AI intelligence briefings"
  default_prompt: "Use $briefwright to demo, configure, preview, run, diagnose, schedule, replay, improve, or safely integrate a Briefwright intelligence briefing. Keep ordinary setup to briefing.yaml, use CLI JSON as the source of truth, protect credentials, and require explicit confirmation for native schedules, policy activation, cadence changes, and knowledge writes."
